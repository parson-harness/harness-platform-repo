# Default values for Harness demo app deployment
# These can be overridden in Harness pipelines using service variables

# Application metadata
name: <+service.name.replace(" ", "").toLowerCase()>
namespace: <+infra.namespace>
replicas: 2
containerPort: 8080

# Harness-provided values
image: <+artifact.image>
service_identifier: <+service.identifier>
env_identifier: <+env.identifier>
build_id: <+pipeline.executionId>

# Application branding
appName: "Harness Demo Application"
appVersion: <+artifact.tag>
customerName: "Harness Customer"
customerLogo: "https://developer.harness.io/img/logo_harness.svg"
deploymentStrategy: "canary"
servicePort: 80
serviceType: ClusterIP

# Deployment variant for blue/green or canary
variant: "canary"
variantColor: "#EAB308"

# Ingress configuration
ingressEnabled: false
ingressDomain: elb.us-east-1.amazonaws.com

# ALB Target Group for automatic pod registration
targetGroupArn: "arn:aws:elasticloadbalancing:us-east-1:759984737373:targetgroup/harness-demo-parson-eks/d020ddacef427f78"

# Resource limits
resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

# Chaos engineering configuration
chaosEnabled: false
chaosLatencyMs: 0
chaosErrorRate: 0.0

# Service configuration
service:
  type: ClusterIP
  port: 80
  annotations: {}

# ServiceMonitor configuration (for Prometheus)
serviceMonitor:
  enabled: true
  toolsNamespace: tools
  releaseLabel: monitoring
  scrapeInterval: 15s
