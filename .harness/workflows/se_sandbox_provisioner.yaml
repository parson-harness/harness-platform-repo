apiVersion: harness.io/v1
kind: Workflow
name: SE Sandbox Provisioner
identifier: se_sandbox_provisioner
type: workflow
scope: project
projectIdentifier: parson
orgIdentifier: sandbox
owner: todd.parson@harness.io
metadata:
  description: |
    Quick provisioning for SE sandbox environments in the Harness demo account.
    
    What gets created:
    - Harness CI pipeline (builds from Harness Code Repo, pushes to HAR)
    - Harness CD pipelines (Canary, Blue-Green, Strategy Choice)
    - Harness Service and Environments (Dev, Prod)
    - K8s namespaces in shared EKS cluster
    - Harness Artifact Registry for Docker images
    
    Prerequisites:
    - Access to sandbox org (you're already here!)
  tags:
    - se-sandbox
    - iacm
    - self-service
    - same-account
spec:
  presentation:
    customTitle: SE Sandbox
    customSubTitle: Create your own demo environment in this Harness account (uses shared EKS cluster)
    customCreateButtonText: Provision Sandbox
  
  output:
    links:
      - title: Pipeline Execution
        url: ${{ steps.trigger.output.PipelineUrl }}
      - title: EKS App URL
        url: http://${{ parameters.owner }}.harness-demo.dev
      - title: ASG App URL
        url: https://${{ parameters.owner }}-asg.harness-demo.dev
      - title: Project Pipelines
        url: https://app.harness.io/ng/account/<+account.identifier>/all/orgs/${{ parameters.harness_org_id }}/projects/${{ parameters.harness_project_id }}/pipelines
      - title: Project Services
        url: https://app.harness.io/ng/account/<+account.identifier>/all/orgs/${{ parameters.harness_org_id }}/projects/${{ parameters.harness_project_id }}/services
      - title: Artifact Registry
        url: https://app.harness.io/ng/account/<+account.identifier>/all/orgs/${{ parameters.harness_org_id }}/projects/${{ parameters.harness_project_id }}/artifacts
  
  lifecycle: production
  
  parameters:
    - title: Sandbox Identity
      required:
        - owner
      properties:
        owner:
          title: Sandbox Owner
          type: string
          description: "Your name (lowercase, alphanumeric). Used for resource naming (e.g., 'todd' creates 'todd_pov' workspace)"
          pattern: "^[a-z][a-z0-9]*$"
          ui:help: "Must start with a letter, lowercase alphanumeric only"
        
        token:
          title: Harness Token
          type: string
          ui:widget: password
          ui:field: HarnessAuthToken
    
    - title: Target Location
      required:
        - harness_org_id
        - harness_project_id
      properties:
        harness_org_id:
          title: Organization
          type: string
          description: "Select the organization for your sandbox"
          default: sandbox
          ui:field: HarnessOrgPicker
        
        harness_project_id:
          title: Project
          type: string
          description: "Select the project for your sandbox"
          default: parson
          ui:field: HarnessProjectPicker
    
    - title: Infrastructure Options
      properties:
        deployment_target:
          title: Deployment Target
          type: string
          description: "Where to deploy the demo app"
          enum:
            - eks
            - asg
          default: eks
        
        eks_cluster_name:
          title: EKS Cluster Name
          type: string
          description: "Name of the shared EKS cluster"
          default: "harness-eks-parson"
    
    - title: Harness Resources
      properties:
        create_harness_environment:
          title: Create Harness Environment?
          type: string
          description: "Create Harness dev environment and infrastructure"
          default: "true"
          enum:
            - "true"
            - "false"
        
        create_prod_environment:
          title: Create Prod Environment?
          type: string
          description: "Create Harness prod environment and infrastructure"
          default: "true"
          enum:
            - "true"
            - "false"
        
        create_standard_ci_gradle:
          title: Create Standard CI Gradle Pipeline?
          type: string
          description: "Enterprise CI pipeline with Test Intelligence, Security Scanning, and Supply Chain"
          default: "true"
          enum:
            - "true"
            - "false"
    
  steps:
    - id: trigger
      name: Provisioning SE Sandbox
      action: trigger:harness-custom-pipeline
      input:
        url: "https://app.harness.io/ng/account/EeRjnXTnS4GrLG5VNNJZUw/all/orgs/sandbox/projects/parson/pipelines/idp_pov_provisioner/pipeline-studio/?storeType=REMOTE&connectorRef=parsongh&repoName=harness-demo-app&branch=main"
        inputset:
          owner: ${{ parameters.owner }}
          harness_account_id: <+account.identifier>
          harness_endpoint: "https://app.harness.io/gratis"
          harness_org_id: ${{ parameters.harness_org_id }}
          harness_project_id: ${{ parameters.harness_project_id }}
          deployment_target: ${{ parameters.deployment_target }}
          eks_cluster_name: ${{ parameters.eks_cluster_name }}
          create_harness_environment: ${{ parameters.create_harness_environment }}
          create_prod_environment: ${{ parameters.create_prod_environment }}
          create_standard_ci_gradle: ${{ parameters.create_standard_ci_gradle }}
        apikey: ${{ parameters.token }}
